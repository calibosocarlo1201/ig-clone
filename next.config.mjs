/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['clipart.info', 'upload.wikimedia.org', 'scontent.fmnl8-1.fna.fbcdn.net'],
    },
};

export default nextConfig;

